{% if theme.settings.get('enable_stock', true) %}
<div class="therwa-stock py-4">
    <div class="container">
        <div class="flex items-center justify-center gap-2 p-4 rounded-lg" style="background-color: #FEE2E2; border: 1px solid #EF4444;">
            <i class="sicon-fire text-lg" style="color: #DC2626;"></i>
            <span class="text-sm font-semibold" style="color: #991B1B;">{{ theme.settings.get('stock_text', '🔥 باقي {quantity} قطع فقط!')|replace({'{quantity}': '5'}) }}</span>
        </div>
    </div>
</div>
{% endif %}
